# H200-only runtime (do not use on non-H200 servers unless explicitly testing).
# Official PyTorch 2.7.0 wheels are cu126/cu128. If the company PyPI/mirror
# provides a custom cu130 build, these plain pins may resolve to that build.
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0
timm>=1.0.9
numpy==1.26.4
pandas>=2.0.0
matplotlib>=3.7.0
Pillow>=10.0.0
tqdm>=4.65.0
pyyaml>=6.0
seaborn>=0.13.0
scipy>=1.10,<1.13
