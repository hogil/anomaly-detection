# Core (데이터 생성 + 학습 + 추론)
# Verified runtime: torch 2.3.1+cu121, torchvision 0.18.1+cu121, numpy 1.26.4.
torch==2.3.1
torchvision==0.18.1
timm>=1.0.9
numpy==1.26.4
pandas>=2.0.0
matplotlib>=3.7.0
Pillow>=10.0.0
tqdm>=4.65.0
pyyaml>=6.0          # imported as `yaml`
seaborn>=0.13.0

# Optional analysis (scripts/analyze_hunting_intervention.py uses scipy.stats).
# Cap <1.13 to avoid the _spropack/_propack ABI breakage seen on Ubuntu 24 numpy 2.x.
scipy>=1.10,<1.13

# Notes:
# - sklearn no longer a runtime dep (numpy-only replacements in train.py/generate_data.py).
# - numpy pinned to 1.26.4 for torch 2.3.x C-API compat.
# - Install everything: `pip install -r requirements.txt` (forgetting one line breaks imports).
