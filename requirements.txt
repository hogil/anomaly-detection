# Core (데이터 생성 + 학습 + 추론)
torch>=2.4.0
torchvision>=0.19.0
timm>=1.0.9
numpy>=1.24.0,<2.2
pandas>=2.0.0
matplotlib>=3.7.0
Pillow>=10.0.0
tqdm>=4.65.0
pyyaml>=6.0
seaborn>=0.13.0

# scipy/sklearn: 더 이상 runtime dep 아님 (numpy-only로 대체됨)
#   - train_test_split → generate_data.stratified_train_val_split
#   - confusion_matrix → train._confusion_matrix_np
# Ubuntu 24 + Python 3.12 + numpy 2.x 환경에서 구버전 scipy/sklearn의
# _spropack/_propack ABI mismatch 이슈를 회피하기 위해 제거됨.
