# Legacy Server Sweeps

This directory contains old broad server grid sweeps from before the current paper pipeline.
They are kept as experiment history, not as the active server entrypoint.

Use `scripts/sweeps_server/` for current paper runs.
